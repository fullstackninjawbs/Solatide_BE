import dotenv from 'dotenv'; dotenv.config(); import mongoose from 'mongoose'; mongoose.connect(process.env.MONGO_URI as string).then(async () => { try { await mongoose.connection.collection('orders').dropIndex('orderNumber_1'); console.log('Dropped orderNumber_1 index from orders'); } catch (e) { console.log('orderNumber_1 not found or dropped'); } try { await mongoose.connection.collection('pages').dropIndex('slug_1'); console.log('Dropped slug_1 index from pages'); } catch (e) { console.log('slug_1 not found or dropped'); } try { await mongoose.connection.collection('products').dropIndex('slug_1'); console.log('Dropped slug_1 index from products'); } catch (e) { console.log('slug_1 not found or dropped'); } mongoose.disconnect(); });